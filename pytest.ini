[pytest]
testpaths = tests
markers =
    unit: Unit tests — fast, isolated, single function or class
    integration: Integration tests — multiple components, HTTP via ASGI transport
    e2e: End-to-end tests — real uvicorn server, real HTTP requests
    slow: Slow running tests
